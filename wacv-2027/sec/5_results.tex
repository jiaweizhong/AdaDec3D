\section{Results}
\label{sec:results}

\subsection{Controlled Reimplementation}

\begin{table}[t]
\centering
\caption{Reproduction fidelity of the matched 3D UX-Net pair, per dataset. We report
mean Dice of our runs against the published \method{} value; $\Delta$ is our
\effimodel{} minus the published \effimodel{}. Data were reconstructed with identity
affine metadata, so this is a controlled reimplementation, not an exact
reproduction.}
\label{tab:baseline}
\setlength{\tabcolsep}{4pt}
\resizebox{\columnwidth}{!}{%
\begin{tabular}{lcccc}
\toprule
Dataset & \fullmodel{} & \effimodel{} & pub.\ \effimodel{} & $\Delta$ \\
\midrule
BTCV (CT, 13-organ) & .792 & .777 & .793 & $-.016$ \\
FeTA (MRI) & -- & -- & -- & -- \\
HepaticVessel (CT) & -- & -- & -- & -- \\
\bottomrule
\end{tabular}}
\todo{Fill FeTA/HepaticVessel rows as the dataset-axis cells complete.}
\end{table}

We reproduce the matched \fullmodel{}/\effimodel{} pair and report its fidelity per
dataset (Table~\ref{tab:baseline}).  On BTCV our \effimodel{} reaches $0.777$ mean
Dice, $1.5$ points below the published $0.793$, while \fullmodel{} is within $0.6$
points of its published value; with \method{}'s default concatenation skip our
\effimodel{} matches its published $3.15$\,M parameter count, so the residual gap
reflects our reconstructed identity-affine data rather than the paper's processed set
(a FP32-versus-BF16 check ruled out a numerical cause).  Efficiency reproduces the
published reductions: \effimodel{} uses $11.7\times$ fewer MACs, $16.8\times$ fewer
parameters, $\sim\!5\times$ lower latency, and $\sim\!7.7\times$ lower peak memory.
The same pattern holds on the architecture axis (Table~\ref{tab:arch}): on BTCV,
Swin UNETR reproduces its published full model ($0.805$ vs.\ $0.8013$) and its
\method{} decoder cuts MACs $6.5\times$ at a $2.6$-point Dice cost---a smaller
reduction than 3D UX-Net's, matching \method{}'s own per-backbone figures.
MedNeXt-M-K3, the strongest backbone at $0.837$ full Dice, shows a $2.3$-point cost at
a $2.2\times$ MAC reduction; across the three backbones the Dice cost is small and of
the same order ($1.5$--$2.6\%$) even as the achievable MAC reduction ranges from
$2.2\times$ to $11.7\times$---consistent across architectures, not universal in
magnitude.
Treating these matched models as fixed, the rest of the paper characterizes
\emph{where} the full decoder's extra computation changes predictions---the question
\method{}'s aggregate evaluation leaves open.

\begin{table}[t]
\centering
\caption{Architecture axis (BTCV): reproduction and efficiency of the matched
full/\method{} pair per backbone. Dice is Full$\rightarrow$\method{}; reductions are
Full$/$\method{}.}
\label{tab:arch}
\setlength{\tabcolsep}{4pt}
\resizebox{\columnwidth}{!}{%
\begin{tabular}{lcccc}
\toprule
Backbone & Dice (F$\rightarrow$E) & MAC$\downarrow$ & Param$\downarrow$ & Lat$\downarrow$ \\
\midrule
3D UX-Net    & .792$\rightarrow$.777 & $11.7\times$ & $16.8\times$ & $5.1\times$ \\
Swin UNETR   & .805$\rightarrow$.779 & $6.5\times$  & $5.7\times$  & $2.3\times$ \\
MedNeXt-M-K3 & .837$\rightarrow$.815 & $2.2\times$ & $3.0\times$ & $2.2\times$ \\
\bottomrule
\end{tabular}}
\end{table}

\subsection{Heterogeneity Metrics}
\label{sec:heterogeneity}

The full decoder's benefit is small and \emph{bidirectional}:
\fullmodel{} corrects $0.278\%$ of \effimodel{}'s voxels ($R_{\mathrm{pos}}$) while
degrading $0.232\%$ ($R_{\mathrm{neg}}$), so the net flip rate $R_{\mathrm{net}}$ is
only $0.047\%$ of voxels---and its subject-level $95\%$ CI, $[-0.026\%, +0.112\%]$,
\emph{includes zero} (Table~\ref{tab:flips}).  With the parameter-matched
\effimodel{}, the full decoder's net benefit is thus not even significantly positive:
reporting positive flips alone would overstate a benefit that, netted against
regressions, is statistically indistinguishable from none.

\begin{table}[t]
\centering
\caption{Prediction-flip rates for the parameter-matched (concatenation) efficient
decoder (fractions of evaluated voxels). Positive/negative flips are the discordant
cells of the paired comparison~\cite{yan2021pct,mcnemar1947}. The net-flip CI
includes zero.}
\label{tab:flips}
\resizebox{0.7\columnwidth}{!}{%
\begin{tabular}{lr}
\toprule
Metric & value\\
\midrule
Positive flip rate $R_{\mathrm{pos}}$ & .00278\\
Negative flip rate $R_{\mathrm{neg}}$ & .00232\\
Net flip rate $R_{\mathrm{net}}$ & .00047\\
Net flip rate, subject 95\% CI & $[-.00026,.00112]$\\
\bottomrule
\end{tabular}}
\end{table}

\begin{figure}[t]
\centering
\includegraphics[width=0.96\columnwidth]{figures/O1_organ_error.png}
\caption{Per-organ error rate for \effimodel{} (seed 1). Difficulty is strongly
uneven across anatomy.}
\label{fig:organ}
\end{figure}

This small net benefit is far from uniformly distributed.  As a first cut,
\effimodel{} error concentrates at structure boundaries ($4.39\times$ the
eroded-interior rate) and on small, hard organs---the adrenals, pancreas, and
veins---with net benefit rising as structure size falls (organ-size/net-flip
$\rho=-0.37$; Fig.~\ref{fig:organ}).  Resolving the \emph{net flip} rate by distance
to the GT boundary shows the decoder's benefit is sharply boundary-localized: it
peaks at the surface ($0.085$ at $0$--$1$ vox) and is near zero or slightly negative
beyond $1$ vox; surface Dice (NSD) is $0.741$/$0.845$ at $\tau{=}1$/$2$~mm.  This
concentration should exceed a same-architecture null pair (Full seed~0 vs.\ seed~1)
\todo{null-pair floor}, ruling out seed noise.

\subsection{Concentration Metrics}
\label{sec:concentration}

\begin{figure}[t]
\centering
\includegraphics[width=0.90\columnwidth]{figures/O2_entropy.png}\\[-1mm]
\textbf{(a) Voxel entropy (log-count).}\\[1mm]
\includegraphics[width=0.96\columnwidth]{figures/O5_decoder_gain.png}\\[-1mm]
\textbf{(b) Net flips vs.\ entropy.}
\caption{Concentration of difficulty and net flips (seed 1). Uncertainty is
spatially sparse (a), and net flips rise with it (b).}
\label{fig:concentration}
\end{figure}

A small, high-uncertainty subset carries most of the net benefit.  Predictive
entropy is spatially sparse: the median voxel entropy is near zero and only $1.26\%$
of voxels exceed $0.5$ (Fig.~\ref{fig:concentration}a).  What net flips there are
concentrate in this tail---the subject-level Pearson correlation between binned
entropy and net flips is $r=0.639$ (95\% CI $[0.198, 0.987]$), bounded above zero
(Fig.~\ref{fig:concentration}b).  A voxel-level oracle makes the concentration
explicit: ranking foreground voxels by entropy, the top $20\%$ contain $83.1\%$ of
all positive flips.

\subsection{Predictability Metrics}
\label{sec:predictability}

\begin{figure}[t]
\centering
\includegraphics[width=0.98\columnwidth]{figures/O9_opportunity_corrected.png}
\caption{Region-level opportunity (seed 1). Contiguous $16^3$ blocks ranked by
entropy recover far more \emph{net} flip utility than a matched-random selector at
every budget $\geq 10\%$; the left panel is a non-deployable voxel oracle.}
\label{fig:opportunity}
\end{figure}

Concentration is only actionable if the beneficial regions can be found without the
ground truth.  We assess this as an error-discrimination problem: predictive entropy
should separate flip voxels from the rest~\cite{hendrycks2017baseline}.
Efficient-decoder entropy predicts the \emph{positive-flip} voxel with per-subject
AUROC $0.903$ ($95\%$ CI $[0.876,0.926]$) and AUPRC $0.299$ ($[0.259,0.342]$);
predicting the error voxel---a cheaper baseline---gives AUROC $0.856$
($[0.789,0.897]$) and AUPRC $0.530$ ($[0.499,0.559]$), and entropy is moderately
calibrated (ECE $0.088$, $[0.065,0.115]$)~\cite{guo2017calibration}.  The pooled-bin
entropy--error correlation $r=0.97$ is retained only as a descriptive diagnostic.

We then convert this into a deployable, selective-prediction analysis
\cite{geifman2017selective}.  Non-overlapping $16^3$ blocks are ranked by mean
entropy and expanded by a $4$-voxel halo; we measure net flip utility recovered
against a matched-random selector under a paired subject bootstrap
(Fig.~\ref{fig:opportunity}).  At the predeclared $20\%$ block budget---executing
$29.1\%$ of the foreground-union volume after halo expansion---entropy-ranked regions
attain a net utility of $0.161$ versus $0.035$ for random ($\sim\!4.6\times$ in the
mean), but under a paired subject bootstrap this advantage is significant only at the
smallest $5\%$ budget (lower bound $0.004$); at every budget $\geq 10\%$ the paired CI
includes zero.  With the parameter-matched decoder the deployable region-level
opportunity is thus real in the mean but marginal in significance---consistent with a
net benefit that is itself barely above zero.

Finally, entropy and confidence are interchangeable routing signals (subject
correlations $0.639$ vs.\ $0.648$, paired difference $\leq 0.02$); Monte-Carlo
dropout is inapplicable at this checkpoint's zero dropout, so we treat confidence as
an equally valid baseline and do not claim entropy is uniquely optimal.

\subsection{Mechanism: Which Factor Is Removable}
\label{sec:mechanism}

The comparisons above use end-to-end models, which share the encoder architecture but
not its weights. As a further, causally cleaner check, we \emph{freeze} one trained
encoder and train the $2\times2$ decoder factorial on identical features, then compare
Full$\rightarrow$channel-only, Full$\rightarrow$resolution-only, and
Full$\rightarrow$combined \method{}
\todo{report per-factor flip rates and boundary profiles from the frozen-encoder run;
hypothesis to test: dropping the high-resolution stage yields flips concentrated
nearer the boundary, while channel reduction is spatially diffuse and smaller, which
would explain why both are removable with little aggregate Dice change}.  Because the
frozen encoder was optimized for the full decoder, a near-match by the efficient
decoder on those features would be a \emph{conservative} statement of redundancy.  We
present this regime as validation of the decoder attribution rather than as the
primary result.
