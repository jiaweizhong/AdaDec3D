\section{Results}
\label{sec:results}

\subsection{Controlled Reimplementation}

\begin{table}[t]
\centering
\caption{Reproduction fidelity of the matched 3D UX-Net pair, per dataset. We report
mean Dice of our runs against the published \method{} value; $\Delta$ is our
\effimodel{} minus the published \effimodel{}. Data were reconstructed with identity
affine metadata, so this is a controlled reimplementation, not an exact
reproduction.}
\label{tab:baseline}
\setlength{\tabcolsep}{4pt}
\resizebox{\columnwidth}{!}{%
\begin{tabular}{lcccc}
\toprule
Dataset & \fullmodel{} & \effimodel{} & pub.\ \effimodel{} & $\Delta$ \\
\midrule
BTCV (CT, 13-organ) & .792 & .773 & .793 & $-.020$ \\
FeTA (MRI) & -- & -- & -- & -- \\
HepaticVessel (CT) & -- & -- & -- & -- \\
\bottomrule
\end{tabular}}
\todo{Fill FeTA/HepaticVessel rows as the dataset-axis cells complete.}
\end{table}

We reproduce the matched \fullmodel{}/\effimodel{} pair and report its fidelity per
dataset (Table~\ref{tab:baseline}).  On BTCV our \effimodel{} reaches $0.773$ mean
Dice over two seeds, $1.7$--$2.2$ points below the published $0.793$, while
\fullmodel{} is within $0.6$ points of its published value; the residual gap reflects
our reconstructed identity-affine data rather than the paper's processed set, and a
FP32-versus-BF16 check ruled out a numerical cause.  Efficiency reproduces the
published reductions: \effimodel{} uses $14.1\times$ fewer MACs, $17.9\times$ fewer
parameters, $\sim\!5\times$ lower latency, and $\sim\!7.7\times$ lower peak memory.
The same pattern holds on the architecture axis (Table~\ref{tab:arch}): on BTCV,
Swin UNETR reproduces its published full model ($0.805$ vs.\ $0.8013$) and its
\method{} decoder cuts MACs $6.5\times$ at a $2.6$-point Dice cost---a smaller
reduction than 3D UX-Net's, matching \method{}'s own per-backbone figures.
MedNeXt-M-K3, the strongest backbone at $0.837$ full Dice, shows the same $2.3$-point
cost at a $2.2\times$ MAC reduction; across all three backbones the Dice cost is
tightly consistent ($-2.2$/$-2.6$/$-2.3\%$) even as the achievable MAC reduction
ranges from $2.2\times$ to $14.1\times$---consistent across architectures, not
universal in magnitude.
Treating these matched models as fixed, the rest of the paper characterizes
\emph{where} the full decoder's extra computation changes predictions---the question
\method{}'s aggregate evaluation leaves open.

\begin{table}[t]
\centering
\caption{Architecture axis (BTCV): reproduction and efficiency of the matched
full/\method{} pair per backbone. Dice is Full$\rightarrow$\method{}; reductions are
Full$/$\method{}.}
\label{tab:arch}
\setlength{\tabcolsep}{4pt}
\resizebox{\columnwidth}{!}{%
\begin{tabular}{lcccc}
\toprule
Backbone & Dice (F$\rightarrow$E) & MAC$\downarrow$ & Param$\downarrow$ & Lat$\downarrow$ \\
\midrule
3D UX-Net    & .792$\rightarrow$.773 & $14.1\times$ & $17.9\times$ & $5.4\times$ \\
Swin UNETR   & .805$\rightarrow$.779 & $6.5\times$  & $5.7\times$  & $2.3\times$ \\
MedNeXt-M-K3 & .837$\rightarrow$.815 & $2.2\times$ & $3.0\times$ & $2.2\times$ \\
\bottomrule
\end{tabular}}
\end{table}

\subsection{Heterogeneity Metrics}
\label{sec:heterogeneity}

The full decoder's benefit is small and \emph{bidirectional}:
\fullmodel{} corrects $0.290\%$ of \effimodel{}'s voxels ($R_{\mathrm{pos}}$) while
degrading $0.225\%$ ($R_{\mathrm{neg}}$) for seed~1, and $0.339\%$/$0.209\%$ for
seed~0, so the net flip rate $R_{\mathrm{net}}$ is only $0.065$--$0.130\%$ of
voxels (Table~\ref{tab:flips}).  Reporting positive flips alone would overstate the
decoder's benefit roughly twofold.

\begin{table}[t]
\centering
\caption{Prediction-flip rates across efficient-decoder seeds (fractions of
evaluated voxels). Positive/negative flips are the discordant cells of the paired
comparison~\cite{yan2021pct,mcnemar1947}.}
\label{tab:flips}
\resizebox{\columnwidth}{!}{%
\begin{tabular}{lrr}
\toprule
Metric & seed 0 & seed 1\\
\midrule
Positive flip rate $R_{\mathrm{pos}}$ & .00339 & .00290\\
Negative flip rate $R_{\mathrm{neg}}$ & .00209 & .00225\\
Net flip rate $R_{\mathrm{net}}$ & .00130 & .00065\\
Net flip rate, subject 95\% CI & -- & $[.00017,.00113]$\\
\bottomrule
\end{tabular}}
\end{table}

\begin{figure}[t]
\centering
\includegraphics[width=0.96\columnwidth]{figures/O1_organ_error.png}
\caption{Per-organ error rate for \effimodel{} (seed 1). Difficulty is strongly
uneven across anatomy.}
\label{fig:organ}
\end{figure}

This small net benefit is far from uniformly distributed.  Error concentrates at
structure boundaries: mean boundary error is $0.1892$ versus $0.0481$ in the eroded
interior, a $3.93\times$ pooled ratio (subject-mean $8.9\times$, $95\%$ CI $[5.8,12.1]$).  It is also
organ-dependent (Fig.~\ref{fig:organ}), with the adrenal glands, pancreas, and
veins hardest, and it scales with structure size: the organ-size/difficulty
Spearman correlation is $\rho=-0.54$, and entropy still explains organ difficulty
after controlling for log volume (partial $r=0.81$).  Decoder benefit is therefore
heterogeneous across region, organ, and scale.  Crucially, resolving flips by
distance to the GT boundary shows the \emph{net flip} rate itself---not just
error---peaks in the boundary \emph{zone} rather than exactly at the surface: it is
near zero at $0$--$1$ vox ($-0.0006$), rises to its maximum of $0.034$ at $1$--$2$
vox, and decays to $0.006$--$0.008$ beyond $4$ vox; surface Dice (NSD) is
$0.745$/$0.842$ at $\tau{=}1$/$2$~mm.  This concentration should exceed a
same-architecture null pair (Full seed~0 vs.\ seed~1) \todo{null-pair floor},
ruling out seed noise.

\subsection{Concentration Metrics}
\label{sec:concentration}

\begin{figure}[t]
\centering
\includegraphics[width=0.90\columnwidth]{figures/O2_entropy.png}\\[-1mm]
\textbf{(a) Voxel entropy (log-count).}\\[1mm]
\includegraphics[width=0.96\columnwidth]{figures/O5_decoder_gain.png}\\[-1mm]
\textbf{(b) Net flips vs.\ entropy.}
\caption{Concentration of difficulty and net flips (seed 1). Uncertainty is
spatially sparse (a), and net flips rise with it (b).}
\label{fig:concentration}
\end{figure}

A small, high-uncertainty subset carries most of the net benefit.  Predictive
entropy is spatially sparse: the median voxel entropy is near zero and only $1.18\%$ of voxels
exceed $0.5$ (Fig.~\ref{fig:concentration}a).  Net flips concentrate in exactly this
tail---the subject-level Pearson correlation between binned entropy and net flips is
$r=0.646$ (95\% CI $[0.158, 0.994]$) for seed~1 and $r=0.665$ ($[0.171,0.996]$) for
seed~0, both bounded above zero (Fig.~\ref{fig:concentration}b).  A voxel-level
oracle makes the concentration explicit: ranking foreground voxels by entropy,
the top $20\%$ contain $86.4\%$ of all positive flips in both seeds.  This
concentration is not a transient of early training: mean entropy falls from
$0.0279$ at 5k iterations to $0.0104$ at 45k, yet the sparse difficult residual
persists.

\subsection{Predictability Metrics}
\label{sec:predictability}

\begin{figure}[t]
\centering
\includegraphics[width=0.98\columnwidth]{figures/O9_opportunity_corrected.png}
\caption{Region-level opportunity (seed 1). Contiguous $16^3$ blocks ranked by
entropy recover far more \emph{net} flip utility than a matched-random selector at
every budget $\geq 10\%$; the left panel is a non-deployable voxel oracle.}
\label{fig:opportunity}
\end{figure}

Concentration is only actionable if the beneficial regions can be found without the
ground truth.  We assess this as an error-discrimination problem: predictive entropy
should separate flip voxels from the rest~\cite{hendrycks2017baseline}.
Efficient-decoder entropy predicts the \emph{positive-flip} voxel with per-subject
AUROC $0.919$ ($95\%$ CI $[0.898,0.936]$) and AUPRC $0.312$ ($[0.269,0.356]$);
predicting the error voxel---a cheaper baseline---gives AUROC $0.892$
($[0.863,0.914]$) and AUPRC $0.578$ ($[0.545,0.613]$), and entropy is moderately
calibrated (ECE $0.097$, $[0.074,0.124]$)~\cite{guo2017calibration}.  The pooled-bin
entropy--error correlation $r=0.97$ is retained only as a descriptive diagnostic.

We then convert this into a deployable, selective-prediction analysis
\cite{geifman2017selective}.  Non-overlapping $16^3$ blocks are ranked by mean
entropy and expanded by a $4$-voxel halo; we measure net flip utility recovered
against a matched-random selector under a paired subject bootstrap
(Fig.~\ref{fig:opportunity}).  At the predeclared $20\%$ block budget---executing
$29.1\%$ of the foreground-union volume after halo expansion---entropy-ranked
regions attain a net utility of $0.225$ versus $0.043$ for random, a roughly
fivefold gain, with a paired $95\%$ lower bound of $0.056>0$; the advantage holds at
every budget $\geq 10\%$ and vanishes only at $5\%$.  These regions capture all
positive flips but also $77.5\%$ of the negative ones, so the \emph{net} gain, not
positive recovery, is the honest headline.

Finally, we compare cheap routing signals against held-out net flips per subject.
Entropy and confidence track net flips almost identically---subject-level
correlations $0.646$ ($[0.158,0.994]$) and $0.651$ ($[0.153,0.997]$), a paired
difference of at most $0.01$ ($[-0.009,-0.002]$)---so they are interchangeable for
routing, and Monte-Carlo dropout is invalid for this checkpoint because
the architecture has no active dropout (its stochastic variance is effectively
zero).  We therefore retain confidence as a required baseline and do not claim
entropy is uniquely optimal.

\subsection{Mechanism: Which Factor Is Removable}
\label{sec:mechanism}

The comparisons above use end-to-end models, which share the encoder architecture but
not its weights. As a further, causally cleaner check, we \emph{freeze} one trained
encoder and train the $2\times2$ decoder factorial on identical features, then compare
Full$\rightarrow$channel-only, Full$\rightarrow$resolution-only, and
Full$\rightarrow$combined \method{}
\todo{report per-factor flip rates and boundary profiles from the frozen-encoder run;
hypothesis to test: dropping the high-resolution stage yields flips concentrated
nearer the boundary, while channel reduction is spatially diffuse and smaller, which
would explain why both are removable with little aggregate Dice change}.  Because the
frozen encoder was optimized for the full decoder, a near-match by the efficient
decoder on those features would be a \emph{conservative} statement of redundancy.  We
present this regime as validation of the decoder attribution rather than as the
primary result.
