# Core DL — PyTorch is pre-installed in the AutoDL image (2.6+/2.8+, CUDA 12.8)
# Install remaining packages:
# The training code passes SwinUNETR(img_size=...), removed in MONAI 1.5.
monai>=1.4,<1.5
timm
batchgenerators
medpy
ptflops
scikit-learn
scipy
nibabel
tqdm
h5py
matplotlib
einops
